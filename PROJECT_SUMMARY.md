# 📦 Preloader Animate v2.0

**Готовый к использованию прелоадер компонент для React проектов**

## 🎯 Что это?

Профессиональный анимированный прелоадер с 4 этапами, который можно легко интегрировать в любой React проект. Построен на Framer Motion с полной поддержкой TypeScript.

## ✨ Основные характеристики

- **Размер**: ~43KB (с зависимостями)
- **Длительность**: 7.5 секунд (настраивается)
- **Технологии**: React 18 + Framer Motion 11 + TypeScript 5
- **Совместимость**: React 18+, Next.js, Gatsby, Vite, CRA

## 📂 Структура проекта

```
preloader-animate/
├── client/src/
│   ├── components/
│   │   └── Preloader.tsx       ← Основной компонент (копируй этот файл)
│   ├── pages/
│   │   └── Home.tsx            ← Пример использования
│   └── index.css               ← Стили (.text-glass-outline)
├── attached_assets/            ← 4 изображения для этапов
├── README.md                   ← Полная документация
├── INTEGRATION.md              ← Быстрая интеграция
└── package.json
```

## 🚀 Быстрый старт (3 шага)

### 1. Запустить демо локально

```bash
npm install
npm run dev
# Откройте http://localhost:5000
```

### 2. Скопировать в свой проект

```bash
# Скопируйте эти файлы:
client/src/components/Preloader.tsx → ваш_проект/src/components/
attached_assets/ → ваш_проект/src/assets/
```

### 3. Использовать

```tsx
import { useState } from "react";
import { Preloader } from "@/components/Preloader";

function App() {
  const [showPreloader, setShowPreloader] = useState(true);

  return (
    <>
      {showPreloader && <Preloader onComplete={() => setShowPreloader(false)} />}
      {!showPreloader && <YourContent />}
    </>
  );
}
```

## 📖 Документация

- **README.md** - Полное руководство по кастомизации
- **INTEGRATION.md** - Пример быстрой интеграции

## 🎨 Что можно настроить

✅ Изображения (4 файла PNG/JPG)  
✅ Тексты этапов ("EXPLORE", "EXPERIENCE", etc.)  
✅ Длительность (по умолчанию 7.5 сек)  
✅ Направления анимации (left/right/top/bottom)  
✅ Шрифты (Google Fonts или локальные)  
✅ Цвета градиента  

## 🛠️ Зависимости

Требуется только:
- `framer-motion` (для анимаций)
- `react` >= 18.0.0
- `tailwindcss` >= 3.0.0

## 📦 Что удалено из оригинального проекта

Для создания чистой версии были удалены:
- ❌ Backend API routes (server/routes.ts, server/storage.ts)
- ❌ База данных (drizzle-orm, PostgreSQL)
- ❌ Формы и подписки (react-hook-form, zod)
- ❌ 40+ UI компонентов (shadcn/ui)
- ❌ Роутинг (wouter)
- ❌ Страница not-found
- ❌ Лишние хуки и утилиты

## ✅ Что осталось

Только необходимое для работы прелоадера:
- ✅ Компонент Preloader.tsx
- ✅ Framer Motion
- ✅ Tailwind CSS
- ✅ Базовые стили (чистый CSS)
- ✅ Простой Express сервер для демо
- ✅ 274 пакета (вместо 477 изначально)

## 🧹 Что удалено дополнительно

- ❌ Все Replit плагины и зависимости
- ❌ Файлы .replit и .local/
- ❌ 40+ UI компонентов из client/src/components/ui/
- ❌ Неиспользуемые хуки из client/src/hooks/
- ❌ Неиспользуемые утилиты из client/src/lib/
- ❌ Лишние CSS переменные и @apply директивы
- ❌ 200+ ненужных npm пакетов

## 📝 Лицензия

MIT - используйте свободно в коммерческих и личных проектах.

---

**🎉 Готово к интеграции!** 

Читайте **README.md** для подробных инструкций.
